# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Billing/Domain/Entity/'
            - '../src/Person/Domain/Entity/'
            - '../src/Notifier/Domain/Entity/'
            - '../src/Kernel.php'

    App\Core\Doctrine\Subscriber\MigrationEventSubscriber:
        tags:
            - { name: doctrine.event_subscriber, connection: default }

    Symfony\Component\Serializer\Normalizer\DateTimeNormalizer:
        arguments:
            $defaultContext:
                datetime_format: 'Y-m-d H:i:s'

    App\Core\Security\TokenGenerator\JwtTokenGenerator:
        arguments:
            $tokenTTL: '%env(JWT_TTL_SEC)%'

    App\Core\Ampq\Producer\EventBus:
        calls:
            - [ 'registerProducer', [ !php/const App\Core\Ampq\Producer\EventBus::CONTRACT_CREATED, '@old_sound_rabbit_mq.contract_created_producer' ] ]
            - [ 'registerProducer', [ !php/const App\Core\Ampq\Producer\EventBus::NOTIFY_PERSON, '@old_sound_rabbit_mq.notify_person_producer' ] ]
