twig:
    file_name_pattern: '*.twig'
    paths:
#        '%kernel.project_dir%/src/Billing/Presentation/Template': 'billing'
#        '%kernel.project_dir%/src/Person/Presentation/Template': 'person'

when@test:
    twig:
        strict_variables: true
