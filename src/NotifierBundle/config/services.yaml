services:
  _defaults:
    autowire: true
    autoconfigure: true

  NotifierBundle\:
    resource: '../../*'
    exclude:
      - '../../Infrastructure/DependencyInjection/'
      - '../../Domain/Entity/'

# Так тоже пробовал, вместо old_sound_rabbit_mq.yaml, начинает не работать на запуске консьюмера
#  old_sound_rabbit_mq:
#    consumers:
#      notify_person:
#        connection: default
#        exchange_options: { name: 'old_sound_rabbit_mq.notify_person', type: direct }
#        queue_options: { name: 'old_sound_rabbit_mq.consumer.notify_person' }
#        callback: NotifierBundle\src\Presentation\Ampq\Consumer\SendEmailNotification\Consumer
#        idle_timeout: 300
#        idle_timeout_exit_code: 0
#        graceful_max_execution:
#          timeout: 1800
#          exit_code: 0
#        qos_options: { prefetch_size: 0, prefetch_count: 1, global: false }
